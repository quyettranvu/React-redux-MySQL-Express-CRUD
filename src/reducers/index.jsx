import { combineReducers } from "redux";
import tutorials from "./tutorials";

//For example, if you have Auth Reducer that manages authentication logic then just add after comma
export default combineReducers({
  tutorials,
});
